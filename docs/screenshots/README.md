# Screenshots

Drop the three README screenshots here as PNG (or WebP):

- `toolbar.png` - the toolbar above the activity table, idle (0 selected).
- `per-row.png` - close-up showing the per-row checkbox and orange GPX button.
- `bulk.png` - mid-bulk-download with the spinner visible, or the resulting zip in the browser's Downloads bar.

The README references them as `docs/screenshots/<name>.png`. If renamed, README should be updated as well.
